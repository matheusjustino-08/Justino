// Clean Embedded Terminal Drawer in Justino (.jucode)

struct TerminalState {
    active_tab: string,
    history_count: int
}

fn initialize(window_ref: int) -> TerminalState {
    return TerminalState {
        active_tab: "terminal",
        history_count: 1
    };
}
